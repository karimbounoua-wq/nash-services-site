<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Nash Services – Réparation Électroménager & Vélos | Marly-Laville</title>
  <meta name="description" content="Nash Services : réparation d’électroménager à domicile et entretien de vélos à Marly-Laville. Intervention rapide sous 48h, sans rendez-vous, 7j/7. Appelez le 07 65 59 70 80.">
  <meta name="keywords" content="réparation électroménager Marly-Laville, entretien vélos Marly-Laville, dépannage électroménager, réparation vélo, Nash Services, Electro Nash, Nash Vélo">

  <!-- Polices modernes (évite Inter/Roboto/Arial) -->
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;600;700;800&family=Barlow+Condensed:wght@500;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --blue: #1d4ed8;
      --blue-dark: #0b1b3f;
      --blue-soft: #1e293b;
      --green: #16a34a;
      --green-soft: rgba(22, 163, 74, 0.18);
      --bg-main: #020617;
      --bg-panel: #020617;
      --text-main: #f9fafb;
      --text-muted: #9ca3af;
      --border-subtle: rgba(148, 163, 184, 0.35);
      --shadow-soft: 0 18px 45px rgba(15, 23, 42, 0.75);
      --radius-lg: 22px;
      --radius-pill: 999px;
      --transition-fast: 0.25s ease;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: "Manrope", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      background: radial-gradient(circle at top, #0b1120 0, #020617 55%, #000 100%);
      color: var(--text-main);
      line-height: 1.6;
      overflow-x: hidden;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    .container {
      max-width: 1120px;
      margin: 0 auto;
      padding: 0 20px;
    }

    /* NAVBAR */
    .navbar {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 50;
      backdrop-filter: blur(18px);
      background: linear-gradient(to bottom, rgba(2, 6, 23, 0.96), rgba(2, 6, 23, 0.8));
      border-bottom: 1px solid rgba(148, 163, 184, 0.25);
    }

    .nav-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 14px 0;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
      font-family: "Barlow Condensed", system-ui;
      letter-spacing: 0.16em;
      font-size: 1.2rem;
      text-transform: uppercase;
    }

    .logo-mark {
      width: 32px;
      height: 32px;
      border-radius: 10px;
      background: linear-gradient(135deg, var(--blue), var(--green));
      box-shadow: 0 0 25px rgba(37, 99, 235, 0.7);
      position: relative;
      overflow: hidden;
    }

    .logo-mark::after {
      content: "";
      position: absolute;
      inset: 4px;
      border-radius: 8px;
      background: radial-gradient(circle at 30% 0, rgba(255, 255, 255, 0.35), transparent 55%);
      mix-blend-mode: screen;
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 26px;
      font-size: 0.9rem;
      text-transform: uppercase;
      letter-spacing: 0.12em;
    }

    .nav-links a {
      position: relative;
      color: var(--text-muted);
      padding-bottom: 4px;
      transition: color var(--transition-fast);
    }

    .nav-links a::after {
      content: "";
      position: absolute;
      left: 0;
      bottom: 0;
      width: 0;
      height: 2px;
      background: linear-gradient(90deg, var(--blue), var(--green));
      transition: width var(--transition-fast);
    }

    .nav-links a:hover {
      color: var(--text-main);
    }

    .nav-links a:hover::after {
      width: 100%;
    }

    .nav-call {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      padding: 10px 18px;
      border-radius: var(--radius-pill);
      background: linear-gradient(135deg, var(--green), #22c55e);
      color: #022c22;
      font-weight: 700;
      font-size: 0.9rem;
      box-shadow: 0 14px 35px rgba(22, 163, 74, 0.55);
      transition: transform var(--transition-fast), box-shadow var(--transition-fast);
    }

    .nav-call span {
      font-family: "Barlow Condensed", system-ui;
      letter-spacing: 0.18em;
      font-size: 0.85rem;
      text-transform: uppercase;
    }

    .nav-call:hover {
      transform: translateY(-2px);
      box-shadow: 0 18px 45px rgba(22, 163, 74, 0.8);
    }

    /* HERO */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding-top: 90px;
      position: relative;
      overflow: hidden;
    }

    .hero-bg-ring {
      position: absolute;
      width: 520px;
      height: 520px;
      border-radius: 999px;
      border: 1px solid rgba(37, 99, 235, 0.35);
      top: -120px;
      right: -120px;
      opacity: 0.6;
      transform: rotate(-8deg);
    }

    .hero-bg-ring::before {
      content: "";
      position: absolute;
      inset: 18%;
      border-radius: inherit;
      border: 1px dashed rgba(37, 99, 235, 0.45);
    }

    .hero-bg-glow {
      position: absolute;
      width: 420px;
      height: 420px;
      border-radius: 999px;
      background: radial-gradient(circle, rgba(37, 99, 235, 0.25), transparent 60%);
      top: 10%;
      right: 5%;
      filter: blur(4px);
    }

    .hero-inner {
      display: grid;
      grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr);
      gap: 60px;
      align-items: center;
      position: relative;
      z-index: 1;
    }

    .hero-kicker {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      padding: 6px 14px;
      border-radius: var(--radius-pill);
      background: rgba(15, 23, 42, 0.9);
      border: 1px solid rgba(148, 163, 184, 0.4);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      color: var(--text-muted);
      margin-bottom: 18px;
    }

    .hero-kicker-dot {
      width: 8px;
      height: 8px;
      border-radius: 999px;
      background: var(--green);
      box-shadow: 0 0 12px rgba(22, 163, 74, 0.9);
    }

    .hero-title {
      font-family: "Barlow Condensed", system-ui;
      letter-spacing: 0.12em;
      font-size: clamp(2.4rem, 4vw, 3.2rem);
      text-transform: uppercase;
      line-height: 1.1;
      margin-bottom: 18px;
    }

    .hero-title span {
      color: var(--green);
    }

    .hero-subtitle {
      font-size: 0.98rem;
      color: var(--text-muted);
      max-width: 460px;
      margin-bottom: 26px;
    }

    .hero-badges {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-bottom: 26px;
    }

    .hero-badge {
      padding: 6px 12px;
      border-radius: var(--radius-pill);
      border: 1px solid rgba(148, 163, 184, 0.35);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      color: var(--text-muted);
    }

    .hero-actions {
      display: flex;
      flex-wrap: wrap;
      gap: 14px;
      align-items: center;
    }

    .btn-primary {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      padding: 14px 26px;
      border-radius: var(--radius-pill);
      border: none;
      background: linear-gradient(135deg, var(--green), #22c55e);
      color: #022c22;
      font-weight: 700;
      font-size: 0.95rem;
      box-shadow: 0 18px 45px rgba(22, 163, 74, 0.6);
      cursor: pointer;
      transition: transform var(--transition-fast), box-shadow var(--transition-fast);
    }

    .btn-primary span {
      font-family: "Barlow Condensed", system-ui;
      letter-spacing: 0.16em;
      font-size: 0.9rem;
      text-transform: uppercase;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 22px 55px rgba(22, 163, 74, 0.85);
    }

    .btn-secondary {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 12px 20px;
      border-radius: var(--radius-pill);
      border: 1px solid rgba(148, 163, 184, 0.5);
      background: rgba(15, 23, 42, 0.8);
      color: var(--text-muted);
      font-size: 0.85rem;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      cursor: pointer;
      transition: background var(--transition-fast), border-color var(--transition-fast), color var(--transition-fast), transform var(--transition-fast);
    }

    .btn-secondary:hover {
      background: rgba(15, 23, 42, 0.98);
      border-color: var(--blue);
      color: var(--text-main);
      transform: translateY(-1px);
    }

    .hero-meta {
      margin-top: 18px;
      font-size: 0.8rem;
      color: var(--text-muted);
    }

    .hero-meta strong {
      color: var(--green);
      font-weight: 700;
    }

    /* HERO RIGHT PANEL */
    .hero-panel {
      background: radial-gradient(circle at top left, rgba(37, 99, 235, 0.18), rgba(15, 23, 42, 0.98));
      border-radius: 26px;
      border: 1px solid rgba(148, 163, 184, 0.35);
      box-shadow: var(--shadow-soft);
      padding: 20px 20px 22px;
      position: relative;
      overflow: hidden;
    }

    .hero-panel-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 14px;
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      color: var(--text-muted);
    }

    .hero-panel-tag {
      padding: 4px 10px;
      border-radius: var(--radius-pill);
      background: rgba(15, 23, 42, 0.9);
      border: 1px solid rgba(148, 163, 184, 0.4);
    }

    .hero-panel-body {
      display: grid;
      grid-template-columns: 1.1fr 1fr;
      gap: 14px;
      align-items: stretch;
    }

    .hero-service-block {
      background: rgba(15, 23, 42, 0.98);
      border-radius: 18px;
      padding: 14px 14px 16px;
      border: 1px solid rgba(148, 163, 184, 0.4);
      position: relative;
      overflow: hidden;
    }

    .hero-service-label {
      font-family: "Barlow Condensed", system-ui;
      letter-spacing: 0.16em;
      font-size: 0.8rem;
      text-transform: uppercase;
      color: var(--green);
      margin-bottom: 4px;
    }

    .hero-service-title {
      font-size: 0.95rem;
      font-weight: 700;
      margin-bottom: 4px;
    }

    .hero-service-text {
      font-size: 0.8rem;
      color: var(--text-muted);
      margin-bottom: 10px;
    }

    .hero-service-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      font-size: 0.7rem;
      color: var(--text-muted);
    }

    .hero-service-meta span {
      padding: 4px 8px;
      border-radius: var(--radius-pill);
      background: rgba(15, 23, 42, 0.9);
      border: 1px solid rgba(148, 163, 184, 0.35);
    }

    .hero-panel-footer {
      margin-top: 14px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 0.78rem;
      color: var(--text-muted);
    }

    .hero-phone {
      font-family: "Barlow Condensed", system-ui;
      letter-spacing: 0.18em;
      font-size: 0.9rem;
      text-transform: uppercase;
      color: var(--green);
    }

    /* SECTIONS */
    section {
      padding: 80px 0;
      position: relative;
    }

    .section-header {
      text-align: left;
      margin-bottom: 40px;
    }

    .section-kicker {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      color: var(--text-muted);
      margin-bottom: 8px;
    }

    .section-title {
      font-family: "Barlow Condensed", system-ui;
      letter-spacing: 0.14em;
      font-size: 1.6rem;
      text-transform: uppercase;
      margin-bottom: 8px;
    }

    .section-subtitle {
      font-size: 0.95rem;
      color: var(--text-muted);
      max-width: 520px;
    }

    /* SERVICES */
    .services {
      background: radial-gradient(circle at top, #0b1120 0, #020617 55%);
    }

    .services-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
      gap: 26px;
      align-items: stretch;
    }

    .service-card {
      background: var(--bg-panel);
      border-radius: var(--radius-lg);
      border: 1px solid var(--border-subtle);
      padding: 22px 22px 24px;
      box-shadow: var(--shadow-soft);
      position: relative;
      overflow: hidden;
    }

    .service-card::before {
      content: "";
      position: absolute;
      inset: -40%;
      background: radial-gradient(circle at top left, rgba(37, 99, 235, 0.18), transparent 60%);
      opacity: 0;
      transition: opacity var(--transition-fast), transform var(--transition-fast);
      transform: translate3d(-10px, -10px, 0);
    }

    .service-card:hover::before {
      opacity: 1;
      transform: translate3d(0, 0, 0);
    }

    .service-label {
      font-family: "Barlow Condensed", system-ui;
      letter-spacing: 0.16em;
      font-size: 0.8rem;
      text-transform: uppercase;
      color: var(--blue);
      margin-bottom: 6px;
    }

    .service-title {
      font-size: 1.1rem;
      font-weight: 700;
      margin-bottom: 6px;
    }

    .service-text {
      font-size: 0.9rem;
      color: var(--text-muted);
      margin-bottom: 14px;
    }

    .service-features {
      list-style: none;
      margin: 0;
      padding: 0;
      display: grid;
      gap: 6px;
      font-size: 0.85rem;
      color: var(--text-muted);
    }

    .service-features li {
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .service-features li::before {
      content: "";
      width: 7px;
      height: 7px;
      border-radius: 999px;
      background: var(--green);
      box-shadow: 0 0 10px rgba(22, 163, 74, 0.9);
    }

    .service-price {
      margin-top: 14px;
      font-size: 0.9rem;
      color: var(--text-muted);
    }

    .service-price strong {
      color: var(--green);
      font-size: 1.1rem;
    }

    .service-note {
      margin-top: 8px;
      font-size: 0.8rem;
      color: var(--text-muted);
      opacity: 0.85;
    }

    /* TARIFS */
    .pricing {
      background: #020617;
    }

    .pricing-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 22px;
    }

    .pricing-card {
      background: var(--bg-panel);
      border-radius: var(--radius-lg);
      border: 1px solid var(--border-subtle);
      padding: 20px 20px 22px;
      position: relative;
      overflow: hidden;
    }

    .pricing-card.featured {
      background: radial-gradient(circle at top left, rgba(22, 163, 74, 0.2), #020617);
      border-color: rgba(22, 163, 74, 0.7);
      box-shadow: 0 20px 55px rgba(22, 163, 74, 0.45);
    }

    .pricing-name {
      font-family: "Barlow Condensed", system-ui;
      letter-spacing: 0.16em;
      font-size: 0.85rem;
      text-transform: uppercase;
      color: var(--text-muted);
      margin-bottom: 6px;
    }

    .pricing-title {
      font-size: 1.05rem;
      font-weight: 700;
      margin-bottom: 10px;
    }

    .pricing-value {
      font-size: 1.8rem;
      font-weight: 800;
      margin-bottom: 6px;
    }

    .pricing-value span {
      font-size: 0.9rem;
      font-weight: 400;
      color: var(--text-muted);
      margin-left: 4px;
    }

    .pricing-list {
      list-style: none;
      margin: 10px 0 0;
      padding: 0;
      display: grid;
      gap: 6px;
      font-size: 0.85rem;
      color: var(--text-muted);
    }

    .pricing-list li {
      display: flex;
      gap: 8px;
    }

    .pricing-list li::before {
      content: "—";
      color: var(--blue);
    }

    .pricing-phone {
      font-size: 1.1rem;
      font-weight: 700;
      margin-top: 6px;
      color: var(--green);
    }

    .pricing-note {
      font-size: 0.8rem;
      color: var(--text-muted);
      margin-top: 4px;
    }

    /* CONTACT */
    .contact {
      background: radial-gradient(circle at top, #0b1120 0, #020617 55%);
    }

    .contact-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
      gap: 26px;
      align-items: start;
    }

    .contact-panel {
      background: var(--bg-panel);
      border-radius: var(--radius-lg);
      border: 1px solid var(--border-subtle);
      padding: 22px 22px 24px;
      box-shadow: var(--shadow-soft);
    }

    .contact-label {
      font-family: "Barlow Condensed", system-ui;
      letter-spacing: 0.16em;
      font-size: 0.8rem;
      text-transform: uppercase;
      color: var(--text-muted);
      margin-bottom: 6px;
    }

    .contact-phone {
      font-size: 1.6rem;
      font-weight: 800;
      letter-spacing: 0.08em;
      margin-bottom: 6px;
      color: var(--green);
    }

    .contact-tagline {
      font-size: 0.9rem;
      color: var(--text-muted);
      margin-bottom: 16px;
    }

    .contact-meta {
      display: grid;
      gap: 10px;
      font-size: 0.85rem;
      color: var(--text-muted);
    }

    .contact-meta strong {
      color: var(--text-main);
    }

    .contact-cta {
      margin-top: 18px;
    }

    .contact-cta .btn-primary {
      width: 100%;
      justify-content: center;
    }

    .contact-side {
      font-size: 0.9rem;
      color: var(--text-muted);
    }

    .contact-side-block {
      margin-bottom: 16px;
    }

    .contact-side-block h3 {
      font-size: 0.9rem;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      font-family: "Barlow Condensed", system-ui;
      margin-bottom: 6px;
      color: var(--text-main);
    }

    /* FOOTER */
    .footer {
      background: #020617;
      border-top: 1px solid rgba(148, 163, 184, 0.25);
      padding: 18px 0 22px;
      font-size: 0.8rem;
      color: var(--text-muted);
      text-align: center;
    }

    .footer strong {
      color: var(--green);
    }

    /* ANIMATIONS */
    .reveal {
      opacity: 0;
      transform: translateY(26px);
      transition: opacity 0.7s ease, transform 0.7s ease;
    }

    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }

    .delay-1 { transition-delay: 0.08s; }
    .delay-2 { transition-delay: 0.16s; }
    .delay-3 { transition-delay: 0.24s; }

    /* RESPONSIVE */
    @media (max-width: 900px) {
      .nav-links {
        display: none;
      }

      .hero-inner,
      .services-grid,
      .contact-grid {
        grid-template-columns: minmax(0, 1fr);
      }

      .hero {
        padding-top: 86px;
      }

      .hero-panel {
        margin-top: 18px;
      }
    }

    @media (max-width: 600px) {
      .hero-title {
        font-size: 2.1rem;
      }

      .hero-subtitle {
        font-size: 0.9rem;
      }

      section {
        padding: 70px 0;
      }
    }
  </style>
</head>
<body>
  <!-- NAVBAR -->
  <header class="navbar">
    <div class="container nav-inner">
      <a href="#top" class="logo">
        <div class="logo-mark"></div>
        <span>NASH SERVICES</span>
      </a>
      <nav class="nav-links">
        <a href="#services">Services</a>
        <a href="#tarifs">Tarifs</a>
        <a href="#contact">Contact</a>
        <a href="tel:0765597080" class="nav-call">
          <span>APPELER</span>
          <strong>07 65 59 70 80</strong>
        </a>
      </nav>
    </div>
  </header>

  <!-- HERO -->
  <main id="top">
    <section class="hero">
      <div class="hero-bg-ring"></div>
      <div class="hero-bg-glow"></div>

      <div class="container hero-inner">
        <div class="hero-left reveal">
          <div class="hero-kicker">
            <div class="hero-kicker-dot"></div>
            <span>Réparation locale – Marly-Laville</span>
          </div>
          <h1 class="hero-title">
            Réparation <span>Électroménager</span><br />
            & <span>Vélos</span> sur un seul numéro
          </h1>
          <p class="hero-subtitle">
            Nash Services intervient rapidement pour vos appareils électroménagers et vos vélos.
            Intervention sous 48h, sans rendez-vous, 7j/7. Un artisan, deux expertises, un contact direct :
            <strong>07 65 59 70 80</strong>.
          </p>

          <div class="hero-badges">
            <div class="hero-badge">Intervention rapide &lt; 48h</div>
            <div class="hero-badge">Sans rendez-vous</div>
            <div class="hero-badge">Note Google ⭐⭐⭐⭐⭐</div>
          </div>

          <div class="hero-actions">
            <a href="tel:0765597080" class="btn-primary">
              <span>APPELER MAINTENANT</span>
              <strong>07 65 59 70 80</strong>
            </a>
            <a href="#services" class="btn-secondary">
              Découvrir les services
            </a>
          </div>

          <p class="hero-meta">
            <strong>Électroménager :</strong> dépannage à domicile •
            <strong>Vélos :</strong> entretien & réparation •
            <strong>Zone :</strong> Marly-Laville & alentours
          </p>
        </div>

        <aside class="hero-panel reveal delay-2">
          <div class="hero-panel-header">
            <span>Vue rapide</span>
            <div class="hero-panel-tag">Deux métiers, un seul numéro</div>
          </div>

          <div class="hero-panel-body">
            <div class="hero-service-block">
              <div class="hero-service-label">Electro Nash</div>
              <div class="hero-service-title">Réparation d’électroménager à domicile</div>
              <p class="hero-service-text">
                Réfrigérateurs, lave-linge, lave-vaisselle, fours, plaques de cuisson.
                Diagnostic et réparation directement chez vous.
              </p>
              <div class="hero-service-meta">
                <span>Intervention sous 48h</span>
                <span>Sans rendez-vous</span>
                <span>À partir de 60€</span>
              </div>
            </div>

            <div class="hero-service-block">
              <div class="hero-service-label">Nash Vélo</div>
              <div class="hero-service-title">Entretien & réparation de vélos</div>
              <p class="hero-service-text">
                Révision complète, réparations toutes marques, réglages précis, personnalisation.
                Votre vélo traité comme en atelier spécialisé.
              </p>
              <div class="hero-service-meta">
                <span>Entretien complet</span>
                <span>Réparation express</span>
                <span>Note Google ⭐⭐⭐⭐⭐</span>
              </div>
            </div>
          </div>

          <div class="hero-panel-footer">
            <div>
              <div>Contact direct</div>
              <div class="hero-phone">07 65 59 70 80</div>
            </div>
            <div>
              <div>Zone</div>
              <div>Marly-Laville & communes voisines</div>
            </div>
          </div>
        </aside>
      </div>
    </section>

    <!-- SERVICES -->
    <section class="services" id="services">
      <div class="container">
        <header class="section-header reveal">
          <div class="section-kicker">Services</div>
          <h2 class="section-title">Deux expertises, un seul interlocuteur</h2>
          <p class="section-subtitle">
            Nash Services regroupe la réparation d’électroménager à domicile et l’entretien de vélos.
            Vous appelez, on s’occupe du reste.
          </p>
        </header>

        <div class="services-grid">
          <article class="service-card reveal delay-1">
            <div class="service-label">Electro Nash</div>
            <h3 class="service-title">Réparation d’électroménager à domicile</h3>
            <p class="service-text">
              Votre appareil est en panne ? Nous intervenons directement chez vous pour diagnostiquer
              et réparer rapidement, avec des explications claires et des tarifs annoncés avant toute intervention.
            </p>
            <ul class="service-features">
              <li>Intervention rapide sous 48h selon disponibilité</li>
              <li>Sans rendez-vous pour les urgences</li>
              <li>Déplacement & diagnostic sur place</li>
              <li>Réfrigérateurs, lave-linge, lave-vaisselle</li>
              <li>Fours, plaques de cuisson et plus</li>
            </ul>
            <p class="service-price">
              À partir de <strong>60€</strong> (déplacement & diagnostic inclus, hors pièces).
            </p>
            <p class="service-note">
              Si des pièces sont nécessaires, elles sont toujours annoncées et validées avec vous avant commande.
            </p>
          </article>

          <article class="service-card reveal delay-2">
            <div class="service-label">Nash Vélo</div>
            <h3 class="service-title">Entretien & réparation de vélos</h3>
            <p class="service-text">
              Pour un vélo sûr, fluide et agréable à rouler. De la simple crevaison à la révision complète,
              votre vélo est pris en charge avec le même sérieux qu’en atelier spécialisé.
            </p>
            <ul class="service-features">
              <li>Entretien complet & réglages précis</li>
              <li>Réparation toutes marques</li>
              <li>Freins, transmission, roues, direction</li>
              <li>Personnalisation & upgrades</li>
              <li>Qualité reconnue – avis clients ⭐⭐⭐⭐⭐</li>
            </ul>
            <p class="service-price">
              Tarifs <strong>sur devis</strong> selon l’état du vélo et les travaux à réaliser.
            </p>
            <p class="service-note">
              Vous décrivez le problème, nous proposons la solution la plus adaptée – jamais de surprise sur la facture.
            </p>
          </article>
        </div>
      </div>
    </section>

    <!-- TARIFS -->
    <section class="pricing" id="tarifs">
      <div class="container">
        <header class="section-header reveal">
          <div class="section-kicker">Tarifs</div>
          <h2 class="section-title">Des prix clairs, annoncés avant intervention</h2>
          <p class="section-subtitle">
            Chaque situation est différente, mais la règle reste la même : transparence, explications,
            et accord avant toute réparation.
          </p>
        </header>

        <div class="pricing-grid">
          <article class="pricing-card reveal delay-1">
            <div class="pricing-name">Électroménager</div>
            <h3 class="pricing-title">Intervention à domicile</h3>
            <div class="pricing-value">
              60€<span>minimum</span>
            </div>
            <ul class="pricing-list">
              <li>Déplacement inclus</li>
              <li>Diagnostic sur place</li>
              <li>Réparation si possible immédiatement</li>
              <li>Pièces facturées uniquement si nécessaires</li>
              <li>Intervention sous 48h selon disponibilité</li>
            </ul>
          </article>

          <article class="pricing-card featured reveal delay-2">
            <div class="pricing-name">Contact direct</div>
            <h3 class="pricing-title">Un seul numéro pour tout</h3>
            <div class="pricing-phone">07 65 59 70 80</div>
            <p class="pricing-note">
              Appelez pour expliquer votre problème : électroménager ou vélo, nous vous indiquons immédiatement
              la marche à suivre et une estimation.
            </p>
            <p class="pricing-note">
              Pas de plateforme, pas de centre d’appel – vous parlez directement avec la personne qui intervient.
            </p>
          </article>

          <article class="pricing-card reveal delay-3">
            <div class="pricing-name">Vélos</div>
            <h3 class="pricing-title">Entretien & réparation</h3>
            <div class="pricing-value">
              Sur devis<span>selon travaux</span>
            </div>
            <ul class="pricing-list">
              <li>Révision complète</li>
              <li>Réparation ciblée (freins, vitesses, roues…)</li>
              <li>Montage & réglage de pièces neuves</li>
              <li>Personnalisation & upgrades</li>
              <li>Conseils d’entretien adaptés à votre usage</li>
            </ul>
          </article>
        </div>
      </div>
    </section>

    <!-- CONTACT -->
    <section class="contact" id="contact">
      <div class="container">
        <header class="section-header reveal">
          <div class="section-kicker">Contact</div>
          <h2 class="section-title">Parlez directement à Nash Services</h2>
          <p class="section-subtitle">
            Un appareil en panne, un vélo qui a besoin d’attention, une question ?
            Un appel suffit pour lancer l’intervention.
          </p>
        </header>

        <div class="contact-grid">
          <div class="contact-panel reveal delay-1">
            <div class="contact-label">Téléphone</div>
            <a href="tel:0765597080" class="contact-phone">07 65 59 70 80</a>
            <p class="contact-tagline">
              Disponible 7j/7 pour vos urgences et vos questions.
            </p>

            <div class="contact-meta">
              <p><strong>Zone d’intervention :</strong> Marly-Laville et communes environnantes.</p>
              <p><strong>Électroménager :</strong> intervention à domicile, diagnostic sur place.</p>
              <p><strong>Vélos :</strong> entretien et réparation avec approche atelier.</p>
            </div>

            <div class="contact-cta">
              <a href="tel:0765597080" class="btn-primary">
                <span>APPELER MAINTENANT</span>
                <strong>07 65 59 70 80</strong>
              </a>
            </div>
          </div>

          <div class="contact-side reveal delay-2">
            <div class="contact-side-block">
              <h3>Horaires & réactivité</h3>
              <p>
                <strong>7 jours sur 7</strong> pour la prise de contact.<br />
                Les interventions sont planifiées au plus vite, avec une priorité pour les pannes bloquantes
                (réfrigérateur, lave-linge, etc.).
              </p>
            </div>

            <div class="contact-side-block">
              <h3>Façon de travailler</h3>
              <p>
                Explications simples, diagnostic transparent, décision toujours prise avec vous avant toute réparation.
                L’objectif : que vous sachiez exactement ce qui est fait, pourquoi, et pour combien.
              </p>
            </div>

            <div class="contact-side-block">
              <h3>Pourquoi Nash Services ?</h3>
              <p>
                Un interlocuteur unique, qui connaît votre dossier, votre matériel et vos attentes.
                Pas de chaîne anonyme : un artisan qui s’engage sur la qualité de son travail.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="container">
      <p>
        © 2026 <strong>Nash Services</strong> – Réparation d’électroménager & vélos | Marly-Laville.
      </p>
      <p>Service professionnel, transparent et de qualité • 07 65 59 70 80</p>
    </div>
  </footer>

  <script>
    // Révélations au scroll (stagger simple)
    const reveals = document.querySelectorAll(".reveal");
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("visible");
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.18 }
    );

    reveals.forEach((el) => observer.observe(el));

    // Tracking simple des clics sur le téléphone (console)
    document.querySelectorAll('a[href^="tel:"]').forEach((link) => {
      link.addEventListener("click", () => {
        console.log("Numéro appelé :", link.getAttribute("href"));
      });
    });
  </script>
</body>
</html>
